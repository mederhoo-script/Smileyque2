rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Product images: publicly readable; only authenticated users may
    // write.  Admin-only access is enforced at the app routing layer.
    // Image must be ≤ 10 MB and have an image/* content-type.
    match /products/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null
                   && request.resource.size <= 10 * 1024 * 1024
                   && request.resource.contentType.matches('image/.*');
    }
  }
}
